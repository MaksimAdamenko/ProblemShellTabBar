<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ProblemShellTabBar.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ProblemShellTabBar"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent Title="Main1"
                      Icon="dotnet_bot"
                      ContentTemplate="{DataTemplate local:MainPage}"/>

        <ShellContent Title="Main2"
                      Icon="dotnet_bot"
                      ContentTemplate="{DataTemplate local:MainPage}"/>

        <ShellContent Title="Main3"
                      Icon="dotnet_bot"
                      ContentTemplate="{DataTemplate local:MainPage}"/>
    </TabBar>

</Shell>
